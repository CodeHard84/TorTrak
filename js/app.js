// TorTrak Source
// Filename: app.js

'use strict';

function AppSate() {
  this.storms = [];
}

AppSate.prototype.loadStorms = function () {
  // Load all storms from storms.json if
  // allStorms key does not exist in storage.
};
